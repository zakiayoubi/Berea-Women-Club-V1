<%- include('partials/header.ejs'); -%>
<div class="container mt-5">
    <!-- Search bar -->
    <form action="/donationsearch" method="get" class="mb-4">
        <div class="row align-items-center">
            <div class="col-md-8">
                <div class="input-group">
                    <input type="search" class="form-control rounded-3" id="searchInput" name="searchTerm" placeholder="Search for a donation inflow">
                    <button class="btn btn-primary" type="submit">Search</button>
                </div>
            </div>
        </div>
    </form>
    <!-- Buttons -->
    <div class="mb-4">
        <button type="button" class="btn btn-secondary me-2" onclick="location.href='/donationInflows/add'">Add Inflow</button>
        <div class="btn-group">
            <button type="button" class="btn btn-secondary dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false">
                Sort By
            </button>
            <ul class="dropdown-menu">
                <li><a class="dropdown-item" href="/sortDonationInflows?sortby=recordName">Record Name</a></li>
                <li><a class="dropdown-item" href="/sortDonationInflows?sortby=category">Category</a></li>
                <li><a class="dropdown-item" href="/sortDonationInflows?sortby=amount">Amount</a></li>
                <li><a class="dropdown-item" href="/sortDonationInflows?sortby=donationDate">Donation Date</a></li>
            </ul>
        </div>
    </div>
    <!-- List of donation inflows -->
    <table class="table">
        <thead>
            <tr>
                <th>ID</th>
                <th>Record Name</th>
                <th>Category</th>
                <th>Amount</th>
                <th>Donation Date</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            <% donationInflow.forEach(donationInflow => { %>
                <tr>
                    <td><%= donationInflow.donationinflowid %></td>
                    <td><%= donationInflow.recordname %></td>
                    <td><%= donationInflow.category %></td>
                    <td>$<%= parseFloat(donationInflow.amount).toFixed(2) %></td>
                    <td><%= new Date(donationInflow.donationdate).toISOString().slice(0, 10) %></td>
                    <td>
                        <!-- Edit and Delete Forms -->
                        <form action="/donationInflows/edit" method="GET" style="display: inline;">
                            <input type="hidden" name="id" value="<%= donationInflow.donationinflowid %>">
                            <button type="submit" class="btn btn-primary btn-sm ms-1">Edit</button>
                        </form>
                        <form action="/delete" method="POST" style="display: inline;">
                            <input type="hidden" name="deleteId" value="<%= donationInflow.donationinflowid %>">
                            <button type="submit" class="btn btn-danger btn-sm" onclick="return confirm('Are you sure you want to delete this item?')">Delete</button>
                        </form>
                    </td>
                </tr>
            <% }); %>
        </tbody>
    </table>
</div>
<%- include('partials/footer.ejs'); -%>
