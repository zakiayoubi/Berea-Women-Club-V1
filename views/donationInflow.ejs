<%- include('partials/header.ejs'); -%>
<div class="container mt-5">
    <!-- Search bar -->
    <div class="p-5 pt-0 half-width">
        <form action="/donationInflows/searchInflow" method="get">
            <div class="row g-1 align-items-center">
                <div class="col-md-8">
                    <div class="form-floating">
                        <input type="search" class="form-control rounded-3" id="searchInput" name="searchTerm" placeholder="Search for a donation inflow">
                        <label for="searchInput">Search for a donation inflow</label>
                    </div>
                </div>
                <div class="col-md-4">
                    <button class="w-100 btn btn-lg rounded-3 btn-primary" type="submit">Search</button>
                </div>
            </div>
        </form>
    </div>


    <!-- Buttons -->
    <div class="mb-3">
        <a href="/donationInflows/addDonationInflow" class="btn btn-secondary">Add Inflow</a>
        <div class="btn-group">
            <button type="button" class="btn btn-secondary dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false">
            Sort By
            </button>
            <form action="/donationInflows/sortInflows" method="POST" class="dropdown-menu">
                <!-- Dropdown menu items as buttons -->
                <button class="dropdown-item" type="submit" name="sortBy" value="recordName">Record Name</button>
                <button class="dropdown-item" type="submit" name="sortBy" value="organization">Organization</button>
                <button class="dropdown-item" type="submit" name="sortBy" value="category">Category</button>
                <button class="dropdown-item" type="submit" name="sortBy" value="amount">Amount</button>
                <button class="dropdown-item" type="submit" name="sortBy" value="donationDate">Date</button>
            </form>
        </div>
    </div>
    <!-- List of donation inflows -->
    <table class="table">
        <thead>
            <tr>
                <th>No</th>
                <th>Record Name</th>
                <th>Organization</th>
                <th>Category</th>
                <th>Amount</th>
                <th>Donation Date</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            <% let counter = 1; %>
            <% donationInflows.forEach(donationInflow => { %>
                <tr>
                    <td scope="row"><%= counter %></td>
                    <td><a href="/donationInflows/<%= donationInflow.donationinflowid %>"><%= donationInflow.recordname %></a></td>
                    <td><%= donationInflow.organizationname %></td> 
                    <td><%= donationInflow.category %></td>
                    <td>$<%= parseFloat(donationInflow.amount).toFixed(2) %></td>
                    <td><%= new Date(donationInflow.donationdate).toISOString().slice(0, 10) %></td>
                    <td>
                        <button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#deleteModal<%= donationInflow.donationinflowid %>">
                            Delete
                        </button>
                        <!-- Modal per member -->
                        <div class="modal fade" id="deleteModal<%= donationInflow.donationinflowid %>" tabindex="-1" aria-labelledby="deleteEvent" aria-hidden="true">
                            <div class="modal-dialog">
                            <div class="modal-content">
                                <div class="modal-header">
                                <h1 class="modal-title fs-5" id="deleteModalLabel">Delete Donation Inflow</h1>
                                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                                </div>
                                <div class="modal-body">
                                Are you sure you want to delete this record?
                                </div>
                                <div class="modal-footer">
                                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                                <form action="/deleteInflow/<%= donationInflow.donationinflowid %>" method="POST">
                                    <button type="submit" class="btn btn-primary">Confirm</button>
                                </form>
                                </div>
                            </div>
                            </div>
                        </div>
                    </td>
                </tr>
                <% counter++; %>
            <% }); %>
        </tbody>
    </table>
</div>
<%- include('partials/footer.ejs'); -%>
